name: Flutter iOS Build for AltStore

on:
  push:
    branches:
      - main
      - master
  pull_request:
    branches:
      - main
      - master
  workflow_dispatch: # Позволяет запускать вручную

jobs:
  build:
    runs-on: macos-latest
    timeout-minutes: 60

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.35.2'
          channel: 'stable'
          cache: true

      - name: Flutter Doctor
        run: flutter doctor -v

      - name: Clean project thoroughly
        run: |
          echo "🧹 Полная очистка проекта..."
          flutter clean
          rm -rf build
          rm -rf ios/Pods 
          rm -rf ios/Podfile.lock
          rm -rf ios/.symlinks
          rm -rf ios/Flutter/Flutter.framework
          rm -rf ~/Library/Developer/Xcode/DerivedData/*
          
      - name: Get Flutter dependencies
        run: |
          echo "📦 Установка Flutter зависимостей..."
          flutter pub get

      - name: Analyze Flutter code
        run: |
          echo "🔍 Анализ кода..."
          flutter analyze --no-fatal-infos || echo "⚠️ Есть предупреждения в коде, но это не критично для сборки"

      - name: Setup CocoaPods
        run: |
          echo "🍎 Настройка CocoaPods..."
          cd ios
          
          # Проверяем версию CocoaPods
          echo "Версия CocoaPods: $(pod --version)"
          
          # Обновляем репозиторий CocoaPods
          echo "Обновление репозитория CocoaPods..."
          pod repo update
          
          # Пробуем установить с основным Podfile
          echo "Попытка установки с основным Podfile..."
          if pod install --repo-update --verbose --clean-install; then
            echo "✅ Основной Podfile установлен успешно"
          else
            echo "⚠️ Ошибка с основным Podfile, переключаемся на минимальный..."
            
            # Бэкап основного Podfile
            cp Podfile Podfile.backup
            
            # Используем минимальный Podfile
            cp Podfile.minimal Podfile
            
            # Очищаем и пробуем снова
            rm -rf Pods Podfile.lock
            
            echo "Установка с минимальным Podfile..."
            if pod install --repo-update --verbose --clean-install; then
              echo "✅ Минимальный Podfile установлен успешно"
            else
              echo "❌ Ошибка даже с минимальным Podfile"
              exit 1
            fi
          fi
          
          # Проверяем результат
          echo "Проверка установленных pods:"
          ls -la Pods/ | head -10

      - name: Check dependencies status
        run: |
          echo "📊 Проверка зависимостей..."
          flutter pub outdated || true
          cd ios && pod outdated || true
          
      - name: Verify iOS project structure
        run: |
          echo "🔍 Проверка структуры iOS проекта..."
          
          # Проверяем ключевые файлы
          echo "Проверка ключевых файлов:"
          ls -la ios/Runner.xcodeproj/project.pbxproj && echo "✅ project.pbxproj найден" || echo "❌ project.pbxproj отсутствует"
          ls -la ios/Runner/Info.plist && echo "✅ Info.plist найден" || echo "❌ Info.plist отсутствует"
          ls -la ios/Pods/ && echo "✅ Pods установлены" || echo "❌ Pods отсутствуют"
          
          # Проверяем Firebase конфигурацию
          echo "Проверка Firebase конфигурации:"
          if [ -f "ios/Runner/GoogleService-Info.plist" ]; then
            echo "✅ GoogleService-Info.plist найден"
          else
            echo "⚠️ GoogleService-Info.plist отсутствует, копируем из корня..."
            if [ -f "google-service-info.json" ]; then
              cp google-service-info.json ios/Runner/GoogleService-Info.plist
              echo "✅ GoogleService-Info.plist скопирован"
            else
              echo "❌ Файл Firebase конфигурации не найден"
            fi
          fi
          
          # Проверяем Flutter конфигурацию
          echo "Проверка Flutter конфигурации:"
          ls -la ios/Flutter/Generated.xcconfig && echo "✅ Generated.xcconfig найден" || echo "❌ Generated.xcconfig отсутствует"
          
          # Проверяем workspace
          echo "Проверка workspace:"
          ls -la ios/Runner.xcworkspace/ && echo "✅ Workspace найден" || echo "❌ Workspace отсутствует"

      - name: Build iOS IPA (unsigned for AltStore)
        run: |
          echo "🔨 Сборка IPA для AltStore..."
          
          # Проверяем, что все готово к сборке
          if [ ! -d "ios/Pods" ]; then
            echo "❌ Ошибка: CocoaPods не установлены"
            exit 1
          fi
          
          # Настраиваем код подписи для unsigned сборки
          echo "⚙️ Настройка для unsigned сборки..."
          
          # Создаем временный xcconfig файл для unsigned сборки
          cat > ios/Flutter/Unsigned.xcconfig << 'EOF'
          #include "Generated.xcconfig"
          CODE_SIGN_IDENTITY=
          CODE_SIGNING_REQUIRED=NO
          CODE_SIGNING_ALLOWED=NO
          PROVISIONING_PROFILE_SPECIFIER=
          DEVELOPMENT_TEAM=
          ENABLE_BITCODE=NO
          SKIP_INSTALL=NO
          EOF
          
          # Обновляем Release.xcconfig для использования unsigned настроек
          cp ios/Flutter/Release.xcconfig ios/Flutter/Release.xcconfig.backup
          cp ios/Flutter/Unsigned.xcconfig ios/Flutter/Release.xcconfig
          
          echo "📋 Проверка настроек перед сборкой..."
          echo "Generated.xcconfig содержимое:"
          cat ios/Flutter/Generated.xcconfig || echo "Generated.xcconfig не найден"
          echo "Release.xcconfig содержимое:"
          cat ios/Flutter/Release.xcconfig
          
          # Проверяем структуру до сборки
          echo "📁 Структура до сборки:"
          ls -la build/ 2>/dev/null || echo "Папка build не существует"
          
          # Запускаем сборку с максимальной детализацией
          echo "🚀 Запуск flutter build ipa с детальным логированием..."
          
          # Устанавливаем переменные окружения для отладки
          export FLUTTER_LOGS_DIR="$PWD/flutter_logs"
          mkdir -p "$FLUTTER_LOGS_DIR"
          
          # Запускаем сборку с перенаправлением вывода
          set -o pipefail
          if flutter build ipa --release --no-codesign --verbose 2>&1 | tee "$FLUTTER_LOGS_DIR/flutter_build_ipa.log"; then
            echo "✅ Команда flutter build ipa завершилась с кодом 0"
            
            # Немедленно проверяем результат
            echo "🔍 Немедленная проверка результата сборки..."
            echo "Структура build/ после flutter build ipa:"
            find build/ -type f -name "*.ipa" -o -name "*.app" -o -name "*.xcarchive" 2>/dev/null || echo "Нет архивов или IPA файлов"
            
            # Проверяем все возможные локации IPA
            echo "Поиск IPA файлов во всех локациях:"
            find . -name "*.ipa" -type f 2>/dev/null || echo "IPA файлы не найдены нигде"
            
            # Проверяем, создался ли IPA в ожидаемом месте
            if [ -f "build/ios/ipa/Runner.ipa" ] || [ -f "build/ios/ipa/taste_smoke_ios.ipa" ] || ls build/ios/ipa/*.ipa 1> /dev/null 2>&1; then
              echo "✅ IPA файл найден в build/ios/ipa/"
            else
              echo "⚠️ IPA не найден в ожидаемом месте, проверяем альтернативные локации..."
              
              # Проверяем архивы Xcode
              echo "Поиск xcarchive файлов:"
              find build/ -name "*.xcarchive" -type d 2>/dev/null || echo "xcarchive не найдены"
              
              # Проверяем, есть ли app bundle
              echo "Поиск app bundle:"
              find build/ -name "*.app" -type d 2>/dev/null || echo "app bundle не найдены"
              
              echo "❌ flutter build ipa не создал IPA файл, переходим к альтернативному методу"
              BUILD_IPA_SUCCESS=false
            fi
          else
            echo "❌ Команда flutter build ipa завершилась с ошибкой"
            BUILD_IPA_SUCCESS=false
          fi
          
          # Альтернативный метод, если основной не сработал
          if [ "$BUILD_IPA_SUCCESS" = "false" ]; then
            echo "🔄 Пробуем альтернативный метод сборки..."
            
            # Восстанавливаем оригинальный файл
            cp ios/Flutter/Release.xcconfig.backup ios/Flutter/Release.xcconfig
            
            # Пробуем сборку iOS app bundle
            echo "📱 Сборка iOS app bundle..."
            if flutter build ios --release --no-codesign --verbose 2>&1 | tee "$FLUTTER_LOGS_DIR/flutter_build_ios.log"; then
              echo "✅ Сборка iOS app завершена успешно"
              
              # Проверяем результат
              echo "Структура build/ios/iphoneos после flutter build ios:"
              ls -la build/ios/iphoneos/ 2>/dev/null || echo "build/ios/iphoneos не существует"
              
              # Создаем IPA вручную из app bundle
              echo "🔧 Создание IPA из app bundle..."
              mkdir -p build/ios/ipa
              
              if [ -d "build/ios/iphoneos" ]; then
                cd build/ios/iphoneos
                
                # Находим .app файл
                APP_NAME=$(find . -name "*.app" -type d | head -1)
                if [ -n "$APP_NAME" ]; then
                  echo "✅ Найден app bundle: $APP_NAME"
                  
                  # Проверяем содержимое app bundle
                  echo "Содержимое app bundle:"
                  ls -la "$APP_NAME"
                  
                  # Создаем структуру для IPA
                  mkdir -p Payload
                  cp -r "$APP_NAME" Payload/
                  
                  # Создаем IPA файл
                  IPA_NAME="taste_smoke_ios.ipa"
                  if zip -r "../ipa/$IPA_NAME" Payload/; then
                    echo "✅ IPA создан вручную: ../ipa/$IPA_NAME"
                    
                    # Проверяем созданный IPA
                    cd ../ipa
                    echo "Размер созданного IPA: $(du -h "$IPA_NAME" | cut -f1)"
                    echo "Содержимое IPA:"
                    unzip -l "$IPA_NAME" | head -10
                    cd ../../..
                  else
                    echo "❌ Ошибка создания ZIP архива"
                    cd ../../..
                    exit 1
                  fi
                else
                  echo "❌ App bundle не найден в build/ios/iphoneos"
                  ls -la build/ios/iphoneos/
                  cd ../../..
                  exit 1
                fi
              else
                echo "❌ Папка build/ios/iphoneos не существует"
                exit 1
              fi
            else
              echo "❌ Ошибка сборки iOS app"
              echo "Сохраняем логи для диагностики..."
              exit 1
            fi
          fi
          
          # Финальная проверка
          echo "🎯 Финальная проверка созданного IPA..."
          if [ -d "build/ios/ipa" ] && ls build/ios/ipa/*.ipa 1> /dev/null 2>&1; then
            echo "✅ IPA файл успешно создан!"
            for ipa in build/ios/ipa/*.ipa; do
              echo "📱 IPA: $ipa"
              echo "   Размер: $(du -h "$ipa" | cut -f1)"
              echo "   Тип файла: $(file "$ipa")"
            done
          else
            echo "❌ IPA файл не был создан ни одним из методов"
            echo "Полная диагностика:"
            find build/ -type f 2>/dev/null | head -20
            exit 1
          fi
          
      - name: Verify IPA creation and validate
        run: |
          echo "🔍 Детальная проверка и валидация IPA..."
          
          # Проверяем структуру build директории
          echo "=== Полная структура build/ ==="
          find build/ -type f 2>/dev/null | sort || echo "Папка build пуста или не существует"
          
          echo ""
          echo "=== Поиск всех IPA файлов ==="
          find . -name "*.ipa" -type f 2>/dev/null || echo "IPA файлы не найдены нигде в проекте"
          
          echo ""
          echo "=== Проверка папки build/ios/ipa ==="
          if [ -d "build/ios/ipa" ]; then
            echo "✅ Папка build/ios/ipa существует"
            ls -la build/ios/ipa/
            
            if ls build/ios/ipa/*.ipa 1> /dev/null 2>&1; then
              echo ""
              echo "✅ IPA файлы найдены! Детальная проверка:"
              
              for ipa in build/ios/ipa/*.ipa; do
                echo ""
                echo "📱 Анализ IPA: $ipa"
                echo "   📏 Размер: $(du -h "$ipa" | cut -f1)"
                echo "   🔍 Тип файла: $(file "$ipa")"
                
                # Проверяем, что это действительно ZIP архив
                if file "$ipa" | grep -q "Zip archive"; then
                  echo "   ✅ Корректный ZIP архив"
                  
                  # Проверяем содержимое IPA
                  echo "   📦 Содержимое IPA (первые 15 файлов):"
                  unzip -l "$ipa" | head -20 | tail -15
                  
                  # Проверяем наличие обязательных компонентов
                  echo "   🔍 Проверка обязательных компонентов:"
                  
                  if unzip -l "$ipa" | grep -q "Payload/.*\.app/"; then
                    echo "   ✅ App bundle найден в Payload/"
                  else
                    echo "   ❌ App bundle не найден в Payload/"
                  fi
                  
                  if unzip -l "$ipa" | grep -q "Payload/.*\.app/Info.plist"; then
                    echo "   ✅ Info.plist найден"
                  else
                    echo "   ❌ Info.plist не найден"
                  fi
                  
                  if unzip -l "$ipa" | grep -q "Payload/.*\.app/.*"; then
                    APP_BUNDLE=$(unzip -l "$ipa" | grep "Payload/.*\.app/" | head -1 | awk '{print $4}' | cut -d'/' -f2)
                    echo "   📱 Имя app bundle: $APP_BUNDLE"
                  fi
                  
                  # Проверяем размер - должен быть больше 1MB для реального приложения
                  SIZE_BYTES=$(stat -f%z "$ipa" 2>/dev/null || stat -c%s "$ipa" 2>/dev/null || echo "0")
                  SIZE_MB=$((SIZE_BYTES / 1024 / 1024))
                  
                  if [ "$SIZE_MB" -gt 1 ]; then
                    echo "   ✅ Размер IPA корректный: ${SIZE_MB}MB"
                  else
                    echo "   ⚠️ IPA слишком маленький: ${SIZE_MB}MB (возможно, неполный)"
                  fi
                  
                else
                  echo "   ❌ Файл не является корректным ZIP архивом"
                  echo "   🔍 Содержимое файла (первые 100 байт):"
                  hexdump -C "$ipa" | head -5
                fi
              done
              
              echo ""
              echo "🎉 Проверка завершена успешно!"
              
            else
              echo "❌ IPA файлы не найдены в папке build/ios/ipa"
              echo "Содержимое папки:"
              ls -la build/ios/ipa/ || echo "Не удается прочитать содержимое папки"
              exit 1
            fi
          else
            echo "❌ Папка build/ios/ipa не существует"
            echo ""
            echo "=== Диагностика отсутствия IPA ==="
            echo "Возможные причины:"
            echo "1. flutter build ipa не завершился успешно"
            echo "2. Проблемы с настройками подписи кода"
            echo "3. Ошибки в Info.plist или bundle identifier"
            echo "4. Недостаточно места на диске"
            echo ""
            echo "Полная структура build/:"
            find build/ -type f 2>/dev/null | head -30 || echo "Папка build не существует"
            exit 1
          fi

      - name: Upload IPA artifact
        uses: actions/upload-artifact@v4
        with:
          name: taste-smoke-ios-altstore
          path: build/ios/ipa/*.ipa
          retention-days: 30

      - name: Debug build failure
        if: failure()
        run: |
          echo "🔍 Диагностика ошибки сборки..."
          
          echo "=== Структура проекта ==="
          ls -la ios/
          
          echo "=== Структура build ==="
          find build/ -type f 2>/dev/null || echo "Папка build не существует"
          
          echo "=== Xcode проект ==="
          ls -la ios/Runner.xcodeproj/
          ls -la ios/Runner.xcworkspace/ 2>/dev/null || echo "Workspace не найден"
          
          echo "=== CocoaPods ==="
          ls -la ios/Pods/ 2>/dev/null || echo "Pods не найдены"
          cat ios/Podfile.lock | head -20 2>/dev/null || echo "Podfile.lock не найден"
          
          echo "=== Flutter конфигурация ==="
          ls -la ios/Flutter/
          cat ios/Flutter/Generated.xcconfig 2>/dev/null || echo "Generated.xcconfig не найден"
          
          echo "=== Последние логи сборки ==="
          find . -name "*.log" -mtime -1 2>/dev/null | head -5 | xargs tail -50 2>/dev/null || echo "Логи не найдены"

      - name: Upload Flutter build logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: flutter-build-logs
          path: |
            flutter_logs/
          retention-days: 7

      - name: Upload build logs (on failure)
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: build-logs-debug
          path: |
            ios/Pods/
            build/
            ios/Flutter/
            ios/Runner.xcodeproj/
            ios/Podfile*
            flutter_logs/
          retention-days: 7
